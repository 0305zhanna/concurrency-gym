package com.epam.concurrencygym.ex7;

/**
 * Make a skaling thread pool executor, which corresponds to the following requirement:
 *  if running tasks < core pool size and queue is empty run a new Thread
 *  instead of putting it to a queue
 */
public class ScalableThreadPoolExecutor {

}
